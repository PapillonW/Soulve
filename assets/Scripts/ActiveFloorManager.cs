using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ActiveFloorManager : MonoBehaviour
{

   /* private void OnTriggerEnter(Collider trigger)
    {
        if (trigger.gameObject.layer == 9)
        {
            trigger.gameObject.layer = 11;
        }
    }

    private void OnTriggerExit(Collider trigger)
    {
        if (trigger.gameObject.layer == 11)
        {
            trigger.gameObject.layer = 9;
        }
    }

    private void OnCollisionEnter(Collision collision)
    {
        if (collision.gameObject.layer == 9 || collision.gameObject.layer == 11)
        {
            collision.gameObject.layer = 10;
        }
    }

    private void OnCollisionExit(Collision collision)
    {
        if (collision.gameObject.layer == 10)
        {
            collision.gameObject.layer = 9 ;
        }
    }*/
}
